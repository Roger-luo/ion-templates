using Documenter
using {{ project.name }}
using DocThemeIndigo

indigo = DocThemeIndigo.install(Configurations)

makedocs(;
    modules = [{{ project.name }}],
    format = Documenter.HTML(
        prettyurls = !("local" in ARGS),
        canonical="https://{{ project.git.user }}.github.io/{{ project.name }}.jl",
        assets=String[indigo],
    ),
    pages = [
        "Home" => "index.md",
    ],
    repo = "{{ repo.url }}",
    sitename = "{{ project.name }}.jl",
)

deploydocs(; repo = "{{ repo.url }}")
